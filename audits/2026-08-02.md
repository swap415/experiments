# audit 2026-08-02 (exp018 v3.1, exp019, exp020, exp021, exp014 3b+3c)

Recompute: 2026-08-02-recompute.py — 34 checks against raw CSVs.
Physics: all exp018 v3.1 rows under f64 peak (84.8); no rate claims
elsewhere this week (compile-time wall-clock only). Parallel workspace
s1-mca-calibration: untouched since 07-24, still stalled.

## findings

1. FIXED — exp014 phase 3b prose/CSV drift (3 mismatches, 0.1-0.6ms):
   results3.md quoted the console run that preceded the CSV-writing
   rerun. Table synced to parfors_gap.csv (the committed artifact is
   authoritative). Recurring lesson now twice-caught this month: when a
   script is edited to add output, the final run's numbers are the
   numbers.
2. FIXED — exp019 evidence versioning: its input CSV was overwritten by
   the exp018 v3.1 rerun. Original archived (data-2026-07-29.csv,
   reproduces the published table exactly; learn.py now takes a path).
   On current data the conclusion strengthens in contrast: learned
   1.086x vs hand rule 1.017x vs probe 1.001x aggregate regret.
3. NEW datum from the reconciliation: stencil d=16 shows 24.6%
   run-to-run throughput difference with IDENTICAL emitted asm across
   configs — code placement/alignment variance. This is the measured
   size of the "statically indistinguishable" noise floor exp015's
   information ceiling predicted, and it bounds what ANY asm-feature
   policy can resolve. Worth a dedicated alignment experiment if the
   paper needs a tight number (s1-mca-calibration's function_address
   metadata was designed for exactly this).
4. 34/34 after fixes. Third consecutive audit with no substantive
   (conclusion-changing) numeric error.

## re-scoring

S2 8.0 -> 8.5: complete arc delivered — mechanism, zero-regression
policy generalized over 4 families (98.6% of oracle), shipped utility
surviving live validation + its own restore-bug. S1 8.5 (complete),
S3 8.0 (ladder complete: 197-276 cold / 161 hit / 34.5 fork-compile /
0.66 fork-cached; remaining work is design), S4a 4.0 (LOFO negative
stands; strengthened by finding 3 — even the hand rule's residual
regret is alignment noise, not learnable signal), S4b 6.0 (still
unexercised — two S4 slots consumed by S4a scoping+LOFO; next S4
session should do actual S4b pattern-mining or the score decays).

## folded into paper drafts

cost-signals-notes.md: exhibits 8 (knob-backfire triad: stencil -83%,
gather -46%, O0 17.5x compile blowup — "less optimization" is never a
free lunch on any measured axis) and 9 (the S3 ladder: every cold-start
strategy quantified; caching ceiling confirmed 18.4% vs predicted 18).
Draft-readiness: evidence inventory is arguably complete for paper 2;
the missing piece is prior-art positioning (uiCA/Ithemal accuracy
tables) and the alignment-noise floor number (finding 3).
