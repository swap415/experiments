# audit 2026-07-28 (exp016, exp014 phase 2+3a, exp017)

Recompute: 2026-07-28-recompute.py — 23/23 claims verified from CSVs.
Physics: exp017 corpus under thresh2k stays below both FMA peaks
(f32 max <= 169.6, f64 max <= 84.8), 0 elision rows. Second consecutive
zero-contradiction audit.

## findings

1. Claims without CSV backing (noted, low severity): exp014
   init_decompose.py and fork_floor.py print measured numbers but write
   no CSV; results2.md quotes them (58.38ms, 0.66ms, 34.5ms). Scripts
   are committed and rerunnable, so claims are reproducible — but the
   audit convention is CSV-first. Convention decision: printed-only is
   acceptable for <=10-number controls; sweeps must write CSV.
2. s1-mca-calibration reconciliation (flagged 2026-07-24): their
   smoke-v1 summary.csv (6 cases, map/reduction/gather, n=1024) shows
   mca_over_measured 1.11-1.30x on vectorized cases — directionally
   consistent with exp016's 1.43x mean overprediction (larger bodies,
   larger corpus). No contradiction. Their harness adds per-branch
   cycles + code alignment metadata exp016 lacks; the multi-family
   corpus overlaps the S4a gate requirement. Recommend: adopt their
   family set into corpus v3; workspace still lacks its bench.py
   (stalled) — do not build on it until its author session returns.

## re-scoring

S2 7.0 -> 8.0: gate opened AND first shipped result in one session
(exp017: 3.2-3.9x cliff fix at negative compile cost, zero-regression
selection policy, layer decision recorded). S1 8.5 (complete as scoped;
future work moves under corpus v3). S3 8.0 (fork floor measured:
0.66ms/34.5ms — the strategy claim is now quantified). S4a 5.5, S4b 6.0
unchanged (S4 session skipped this week — queued prompts collapsed).

Arc verdict strengthens: exp017 is the arc working as designed —
exp013's corpus found the miss, exp016 disqualified static ranking,
exp015's asm features supplied the selection rule, exp017 shipped the
fix. Four experiments, one causal chain.

## folded into paper drafts

cost-signals-notes.md: exp017 added as the constructive exhibit (the
cost-model arc ends in a shipped optimization with a zero-regression
policy). Gap list updated: multi-family corpus now unblocks BOTH the
S4a gate and exp017 generalization.
