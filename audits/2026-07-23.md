# audit 2026-07-23 (exp013 v2, exp014, exp015, ROADMAP)

Recompute script: 2026-07-23-recompute.py — 51 checks, all from raw CSVs.

## verdict: 51/51 claims verified, zero prose/data contradictions

First audit with no numeric findings (previous audits caught 3). The
executed-vs-nominal counter check running inside the harness likely
deserves the credit — errors get caught at measurement time now.

## physics checks

- exp013: max f32 138.9 = 82% of 169.6 peak; max f64 65.0 = 77% of 84.8;
  no row exceeds either. Single pinned core, memory wall not applicable
  (compute-bound corpus, 4M elements).
- exp014: wall times only, no physics bound applies; cross-checked
  compile == compiler_lock (gap <= 0.1ms) and init residue constancy
  (63.3-64.2ms across all 8 kernels).
- exp015: derived entirely from exp013 CSV; no new measurements to check
  beyond costs (2.1us/5.5ms reproduced by learn.py rerun).

## findings (evidence preservation, both resolved)

1. exp014 results.md cited a same-process init control (66.5 -> 1.1ms)
   that existed only in a session heredoc. FIXED: committed
   exp014-compiletime/control_init.py; rerun reproduces 66.5/1.1/1.0ms.
2. exp013 results.md claims "deficit = accs-1 exactly" from an
   intermediate run whose CSV was overwritten by the corrected-nominal
   rerun (same for the "exact 0.500" smoke observation). RESOLVED by
   consistency check on committed data: current ratios are exactly 1.000
   where non-absorption would read 1.059-1.158; the 0.500 run is
   reproducible by reverting the distinct-multiplier edit. Process note
   for future sessions: when a run motivates a harness change, commit or
   copy the motivating CSV before overwriting it.

## re-scoring (applied to ROADMAP.md)

S3 7.5 -> 8.0 (fixed-overhead discovery: bigger, more tractable target
than the seeded pass-pruning framing; that sub-bet demoted — LLVM slice
is 25-46%, caching ceiling 18% for simple kernels). S4a 4.0 -> 5.5
(measured design point; still single-family gated). S1 8.5, S2 7.0,
S4b 6.0 unchanged. Arc: demonstrated (exp015 consumed exp013's corpus);
S3<->S1 coupling weaker than seeded.

## folded into paper drafts

paper/cost-signals-notes.md created — evidence inventory for paper 2
("cheap hardware grounding beats static cost models"): exp005 cliff,
exp013 v2 model table + elision methodology, exp015 learned ceiling +
regret framing. Paper 1 (hybrid chunking) untouched today; no new
evidence for it.
